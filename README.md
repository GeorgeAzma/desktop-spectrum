# Desktop Audio Spectrogram

Display live spectrogram from desktop audio
![spectrogram](spectrogram.png)

### Controls

- `ESC` exit
- `LMB_DRAG` move window
- `RMB_DRAG` resize window
- `SPACE` pause
- `T` toggle always on top
